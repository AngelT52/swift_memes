class Meme{
    let name: String
    let url: String
    let id: String
    let boxCount: String
    
    init(name: String, url: String, id: String, boxCount: String){
        self.name = name
        self.url = url
        self.id = id
        self.boxCount = boxCount
    }
}
